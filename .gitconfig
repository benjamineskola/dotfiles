# vim:ft=gitconfig
[alias]
	st = status -sb
	co = checkout
	ci = commit
	l = log -M --graph --decorate --abbrev-commit
	la = log -M --graph --all --decorate --abbrev-commit
	d = diff -M
	dc = diff --cached -M
	addp = add -p
	amend = commit --amend -CHEAD
	edit-message = commit --amend -m
	undo = checkout -p
[branch]
	autosetuprebase = always
[color]
	diff = auto
	status = auto
	branch = auto
[core]
	excludesfile = ~/.gitignore.global
[diff]
	renames = copy
[push]
	default = simple
[user]
	name = Benjamin M. A'Lee
	email = me@benjaminalee.uk
