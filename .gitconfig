[user]
	name = Benjamin M. A'Lee
	email = bma@bma.cx
	signingkey = 166891C7
[alias]
	st = status
	ci = commit
	logall = log -M --graph --all --decorate
	dw = diff --color-words
	dc = diff --cached
	dwc= diff --cached --color-words
	unstage = reset HEAD --
	not-in = "!sh -c 'for i in `git branch|grep [0-9]|cut -c 2-`; do l=`git log $0..$i|wc -l`; if [ $l -gt 0 ]; then echo $i; fi; done'"
	co = checkout
[color]
	diff = auto
	status = auto
	branch = auto
[merge]
	tool = emerge
[merge "tool"]
	cmd = emacsclient -t -a \"\" -e \"(emerge-files-with-ancestor $LOCAL $REMOTE $BASE $MERGED)\"
