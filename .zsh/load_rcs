# -*- zsh -*-

if [[ -o rcs ]]; then
	source $ZDOTDIR/init

	_basename=${${(%):-%1N}#.}

	source_dir $ZDOTDIR/$_basename
	source_dir $ZDOTDIR/$OS/$_basename
	source_dir $ZDOTDIR/$HOSTNAME/$_basename
	source_dir $ZDOTDIR/after/$_basename

	unset _basename
fi
