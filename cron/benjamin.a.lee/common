GIT_SSH=$HOME/bin/git-ssh-client

*/5 * * * * for i in $HOME/.config $HOME/.config-x11 $HOME/.data $HOME/.private; do test -d $i && test -d $i/.git && cd $i && git pull > /dev/null; if [ $i == $HOME/.data ]; then git add org ledger && git commit -m update. . && git push; fi; done
