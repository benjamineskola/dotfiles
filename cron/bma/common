GIT_SSH=$HOME/bin/git-ssh-client

*/5 * * * * for i in $HOME/bin $HOME/.config $HOME/.config-x11 $HOME/.data $HOME/.private; do test -d $i && test -d $i/.git && cd $i && git pull > /dev/null 2>&1; if [ $i = $HOME/.data ]; then (git add org ledger && git commit -m update. . && git push) > /dev/null 2>&1; fi; done
